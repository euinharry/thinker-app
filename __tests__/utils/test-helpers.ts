/**
 * Test Utilities
 *
 * Shared utilities and helpers for tests.
 */

/**
 * Expected agent names in the system.
 * Used for validation in tests.
 */
export const EXPECTED_AGENT_NAMES = ["leader", "explorer", "thinker", "critic"] as const;

/**
 * Expected agent count in the system.
 */
export const EXPECTED_AGENT_COUNT = 4;
